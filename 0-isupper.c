#include <main.h>
/*
 * main - Prototype: int _isupper(int c);
 * Returns 1 if c is uppercase, Returns 0 otherwise
 * Return 0 always
 *
 */
int main(void)
{
	char c;
	
	c = 'A';
	if {
 	putchar("%c: %d\n", c, _isupper(c));
	
		return(1);
	else
		return(0);
	}
		return(0);

}
